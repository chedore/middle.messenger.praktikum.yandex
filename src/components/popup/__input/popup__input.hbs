<input
  type={{type}}
  class="input popup__input {{#if error}}popup__input-error{{/if}}"
  name={{name}}
  {{#if description}}
    value={{description}}
  {{/if}}
  minLength="2"
  maxLength="40"
  required
/>